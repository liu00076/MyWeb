##业务逻辑接口的生成模板

package ${config.package}.${config.module}.service;

import com.misterfat.presentation.mybatis.service.BaseServiceInterface;

import ${config.package}.${config.module}.module.${tableModel.className};

/**
 * 
 * 说明：处理对${tableModel.comment}的业务操作
 * 
 * @author ${config.author}
 * 
 * @version 1.0
 * 
 * @since ${config.date}
 */
public interface ${tableModel.className}Service extends BaseServiceInterface<${tableModel.className}> {

}

